@using MudBlazor
@code {
    private readonly string[] messages = new[]
    {
        "Your wish is our command. The genie is working on it...",
        "Hold on tight! The genie is conjuring up your request...",
        "Abracadabra! The genie is weaving its magic...",
        "Magic in progress... The genie is on the job!",
        "Hocus Pocus! The genie is making it happen..."
    };

    private string currentMessage;

    protected override void OnInitialized()
    {
        base.OnInitialized();
        Random random = new Random();
        currentMessage = messages[random.Next(messages.Length)];
    }

    [Parameter] public bool IsVisible { get; set; }
}

<div class="loading-overlay" style="@(IsVisible ? "display: flex;" : "display: none;")">
    <div class="loading-content">
        <img src="/assets/img/genie-loading.gif" alt="Genie" class="genie-image" />
        <p class="loading-text"><b>@currentMessage</b></p>
    </div>
</div>

<style>
    .loading-overlay {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 1000;
        background-color: rgba(255, 255, 255, 1); /* Solid white background */
    }

    .loading-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
    }

    .genie-image {
        height: 75px;
        margin-bottom: 16px;
    }

    .loading-text {
        color: #501cab; /* Purple text color */
        font-size: 16px;
    }
</style>
